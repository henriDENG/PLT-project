

Automate(2) = {

	etats = [`0`, `1`, `2`]
		initial = 0
	final= [2]
	transitions=[(0 → 0, `a`, (→,`a`)), 				(0 → 1, `b`, (`a`, →), (→, `b`)),
		(1 → 1, `b`, (`a`, →), (→,` b`)),
		(1 → 2, `c`, (), (`b`, →)), 		(2 → 2, `c`, (), (`b`, →))]
}
